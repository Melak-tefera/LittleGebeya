import 'package:flutter/material.dart';

class Shoping extends StatelessWidget {
const Shoping({super.key});
  @override
  Widget build(BuildContext context) {

    return Scaffold(
      backgroundColor: Colors.white,
      body: Center(child: Text("shoping page")),
    
    );
  }
}